---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="CRO for DTC Brands — Structured Experiments That Lift Revenue | Hichem Bennaceur"
  description="Stop leaving money on your existing traffic. I run structured CRO experiments for DTC brands — hypothesis-driven, statistically valid, and compounding month over month."
  canonicalUrl="https://hichembennaceur.com/dtc"
>

<style>
  @import url('https://api.fontshare.com/v2/css?f[]=satoshi@900,700,500,400&display=swap');

  :root {
    --cream: #F9F8F2;
    --cream-dark: #F2EDE3;
    --orange: #E74A1A;
    --blue: #17488B;
    --text: #101010;
    --muted: #666;
    --border: 1px solid #E5E3DA;
    --radius: 12px;
    --font: 'Satoshi', sans-serif;
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }
  html { scroll-behavior: smooth; }
  body { font-family: var(--font); background: var(--cream); color: var(--text); line-height: 1.6; overflow-x: hidden; }
  img { max-width: 100%; display: block; }
  a { color: inherit; text-decoration: none; }

  /* ─── SCROLL PROGRESS ─── */
  .scroll-progress {
    position: fixed; top: 0; left: 0; height: 3px; width: 0%;
    background: linear-gradient(90deg, var(--orange) 0%, #ff6535 50%, #ff8855 100%);
    box-shadow: 0 0 12px rgba(231,74,26,0.6);
    z-index: 999; pointer-events: none;
  }

  /* ─── NAV ─── */
  .nav {
    position: sticky; top: 0; z-index: 100;
    background: rgba(249,248,242,0.92);
    backdrop-filter: blur(12px);
    border-bottom: var(--border);
    padding: 0 48px; height: 64px;
    display: flex; align-items: center; justify-content: space-between; gap: 24px;
  }
  .nav-logo { font-weight: 700; font-size: 15px; letter-spacing: -0.01em; }
  .nav-logo:hover { color: var(--orange); }
  .nav-links { display: flex; align-items: center; gap: 28px; list-style: none; }
  .nav-links a { font-size: 14px; color: var(--muted); transition: color 0.2s; }
  .nav-links a:hover { color: var(--text); }
  .nav-cta {
    background: var(--orange); color: #fff; font-size: 13px; font-weight: 700;
    padding: 9px 20px; border-radius: 1000px;
    transition: opacity 0.2s, transform 0.2s; white-space: nowrap;
  }
  .nav-cta:hover { opacity: 0.88; transform: translateY(-1px); }

  /* ─── HERO ─── */
  .hero {
    padding: 88px 48px 72px;
    max-width: 960px;
    margin: 0 auto;
    text-align: center;
  }
  .hero-tag {
    display: inline-flex; align-items: center; gap: 8px;
    background: rgba(231,74,26,0.07);
    border: 1px solid rgba(231,74,26,0.18);
    color: var(--orange); font-size: 11px; font-weight: 700;
    letter-spacing: 0.1em; text-transform: uppercase;
    padding: 5px 14px; border-radius: 1000px; margin-bottom: 28px;
  }
  .hero-tag-dot {
    width: 6px; height: 6px; border-radius: 50%;
    background: var(--orange);
    animation: pulseDot 2s infinite;
  }
  @keyframes pulseDot {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.4; transform: scale(0.65); }
  }
  .hero-title {
    font-size: clamp(38px, 6vw, 72px);
    font-weight: 900; letter-spacing: -0.035em; line-height: 1.04;
    color: var(--text); margin-bottom: 24px;
  }
  .hero-title .orange { color: var(--orange); }
  .hero-title .underline-accent {
    text-decoration: underline;
    text-decoration-color: var(--orange);
    text-decoration-thickness: 3px;
    text-underline-offset: 6px;
  }
  .hero-sub {
    font-size: 18px; line-height: 1.72; color: var(--muted);
    max-width: 680px; margin: 0 auto 40px;
  }
  .hero-actions {
    display: flex; align-items: center; justify-content: center;
    gap: 16px; flex-wrap: wrap; margin-bottom: 32px;
  }
  .btn-primary {
    background: var(--orange); color: #fff; font-weight: 700; font-size: 15px;
    padding: 14px 28px; border-radius: 1000px;
    display: inline-flex; align-items: center; gap: 8px;
    transition: opacity 0.2s, transform 0.2s;
    position: relative; overflow: hidden;
  }
  .btn-primary:hover { opacity: 0.88; transform: translateY(-2px); }
  .btn-primary::after {
    content: ''; position: absolute; top: 0; left: -90%; width: 55%; height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.28), transparent);
    transform: skewX(-20deg); pointer-events: none;
  }
  .btn-primary:hover::after { animation: shimmer 0.6s ease forwards; }
  @keyframes shimmer { from { left: -90%; } to { left: 150%; } }
  .btn-ghost {
    font-size: 14px; color: var(--muted);
    display: inline-flex; align-items: center; gap: 6px;
    transition: color 0.2s;
  }
  .btn-ghost:hover { color: var(--text); }
  .trust-bar {
    display: flex; align-items: center; justify-content: center; gap: 8px;
    font-size: 13px; color: var(--muted);
  }
  .trust-stars { color: #F59E0B; letter-spacing: 2px; font-size: 14px; }

  /* ─── SECTION EYEBROW ─── */
  .eyebrow {
    font-size: 11px; font-weight: 700; letter-spacing: 0.1em;
    text-transform: uppercase; color: var(--orange); margin-bottom: 14px;
  }

  /* ─── PAIN SECTION ─── */
  .pain-section {
    background:
      radial-gradient(ellipse 55% 60% at 95% 0%, rgba(231,74,26,0.16), transparent 60%),
      radial-gradient(ellipse 40% 50% at 8% 100%, rgba(23,72,139,0.1), transparent 55%),
      #101010;
    color: #fff; padding: 96px 48px;
  }
  .pain-inner { max-width: 1160px; margin: 0 auto; }
  .pain-title {
    font-size: clamp(30px, 3.8vw, 48px); font-weight: 900;
    letter-spacing: -0.03em; line-height: 1.1; color: #fff;
    max-width: 760px; margin-bottom: 56px;
  }
  .pain-grid {
    display: grid; grid-template-columns: repeat(3, 1fr);
    gap: 2px; margin-bottom: 48px;
  }
  .pain-item {
    padding: 32px 28px;
    border: 1px solid rgba(255,255,255,0.07);
    background: rgba(255,255,255,0.03);
    transition: background 0.25s;
  }
  .pain-item:hover { background: rgba(255,255,255,0.07); }
  .pain-item:nth-child(1) { border-radius: 12px 0 0 0; }
  .pain-item:nth-child(2) { border-radius: 0; }
  .pain-item:nth-child(3) { border-radius: 0 12px 0 0; }
  .pain-item:nth-child(4) { border-radius: 0 0 0 12px; }
  .pain-item:nth-child(5) { border-radius: 0; }
  .pain-item:nth-child(6) { border-radius: 0 0 12px 0; }
  .pain-num {
    font-size: 11px; font-weight: 700; letter-spacing: 0.08em;
    color: rgba(231,74,26,0.65); margin-bottom: 12px;
  }
  .pain-item-title {
    font-size: 16px; font-weight: 800; color: #fff;
    margin-bottom: 10px; line-height: 1.35;
  }
  .pain-item-body {
    font-size: 13.5px; color: rgba(255,255,255,0.48); line-height: 1.72;
  }
  .pain-callout {
    background: rgba(231,74,26,0.09);
    border: 1px solid rgba(231,74,26,0.22);
    border-radius: 12px; padding: 28px 32px;
    display: flex; align-items: flex-start; gap: 16px;
  }
  .pain-callout-icon { font-size: 22px; flex-shrink: 0; margin-top: 2px; }
  .pain-callout-text {
    font-size: 15.5px; color: rgba(255,255,255,0.75); line-height: 1.7;
  }
  .pain-callout-text strong { color: #fff; }

  /* ─── COST STATS ─── */
  .cost-section {
    background: var(--cream-dark); border-top: var(--border); border-bottom: var(--border);
    padding: 88px 48px;
  }
  .cost-inner { max-width: 1160px; margin: 0 auto; }
  .cost-header { text-align: center; margin-bottom: 56px; }
  .cost-title {
    font-size: clamp(26px, 3.2vw, 40px); font-weight: 900;
    letter-spacing: -0.025em; color: var(--text); margin-bottom: 10px;
  }
  .cost-sub { font-size: 15px; color: var(--muted); max-width: 480px; margin: 0 auto; }
  .cost-grid {
    display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px;
  }
  .cost-card {
    background: var(--cream); border: var(--border); border-radius: var(--radius);
    padding: 32px 28px; position: relative; overflow: hidden;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }
  .cost-card::before {
    content: ''; position: absolute;
    top: 0; left: 0; right: 0; height: 3px; background: var(--orange);
  }
  .cost-card-num {
    font-size: clamp(40px, 4.5vw, 56px); font-weight: 900;
    color: var(--orange); letter-spacing: -0.05em; line-height: 1; margin-bottom: 8px;
  }
  .cost-card-title {
    font-size: 15px; font-weight: 700; color: var(--text); margin-bottom: 8px;
  }
  .cost-card-body { font-size: 13px; color: var(--muted); line-height: 1.68; }

  /* ─── SOLUTION SECTION ─── */
  .solution-section {
    background:
      radial-gradient(ellipse 45% 65% at 3% 50%, rgba(231,74,26,0.11), transparent 55%),
      radial-gradient(ellipse 50% 50% at 97% 10%, rgba(23,72,139,0.08), transparent 55%),
      #101010;
    color: #fff; padding: 96px 48px;
  }
  .solution-inner { max-width: 1160px; margin: 0 auto; }
  .solution-header { margin-bottom: 52px; }
  .solution-title {
    font-size: clamp(28px, 3.5vw, 46px); font-weight: 900;
    letter-spacing: -0.03em; line-height: 1.1; color: #fff;
    max-width: 620px; margin-bottom: 14px;
  }
  .solution-sub { font-size: 16px; color: rgba(255,255,255,0.48); max-width: 520px; line-height: 1.65; }
  .solution-grid {
    display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px;
  }
  .solution-card {
    background: rgba(255,255,255,0.05);
    border: 1px solid rgba(255,255,255,0.09);
    border-radius: var(--radius); padding: 32px;
    transition: background 0.25s, border-color 0.25s;
  }
  .solution-card:hover { background: rgba(255,255,255,0.09); border-color: rgba(231,74,26,0.28); }
  .solution-icon {
    width: 46px; height: 46px; border-radius: 10px;
    background: rgba(231,74,26,0.14);
    border: 1px solid rgba(231,74,26,0.22);
    display: flex; align-items: center; justify-content: center;
    font-size: 20px; margin-bottom: 20px;
  }
  .solution-icon svg { width: 22px; height: 22px; stroke: var(--orange); stroke-width: 2; fill: none; stroke-linecap: round; stroke-linejoin: round; }
  .pain-callout-icon svg { width: 26px; height: 26px; stroke: rgba(231,74,26,0.85); stroke-width: 2; fill: none; stroke-linecap: round; stroke-linejoin: round; flex-shrink: 0; }
  .solution-card-title {
    font-size: 18px; font-weight: 800; color: #fff;
    margin-bottom: 10px; line-height: 1.3;
  }
  .solution-card-body {
    font-size: 14px; color: rgba(255,255,255,0.48); line-height: 1.72; margin-bottom: 24px;
  }
  .deliverables {
    list-style: none; display: flex; flex-direction: column; gap: 9px;
    border-top: 1px solid rgba(255,255,255,0.07); padding-top: 20px;
  }
  .deliverables li {
    font-size: 13px; color: rgba(255,255,255,0.62);
    display: flex; align-items: flex-start; gap: 8px; line-height: 1.5;
  }
  .deliverables li::before { content: '→'; color: var(--orange); font-weight: 700; font-size: 12px; flex-shrink: 0; margin-top: 2px; }

  /* ─── PROCESS ─── */
  .process-section {
    background: var(--cream); border-top: var(--border); padding: 96px 48px;
  }
  .process-inner { max-width: 1160px; margin: 0 auto; }
  .process-header { text-align: center; margin-bottom: 64px; }
  .process-title {
    font-size: clamp(26px, 3.2vw, 42px); font-weight: 900;
    letter-spacing: -0.025em; color: var(--text); margin-bottom: 12px;
  }
  .process-sub { font-size: 15px; color: var(--muted); }
  .process-steps {
    display: grid; grid-template-columns: repeat(3, 1fr); gap: 40px;
    position: relative;
  }
  .process-connector {
    position: absolute; top: 35px;
    left: calc(33.33% + 4px); right: calc(33.33% + 4px); height: 2px;
    background: linear-gradient(90deg, var(--orange) 0%, rgba(231,74,26,0.15) 100%);
    pointer-events: none;
  }
  .process-step { text-align: center; }
  .step-num {
    width: 70px; height: 70px; border-radius: 50%;
    background: var(--orange); color: #fff;
    font-size: 22px; font-weight: 900;
    display: flex; align-items: center; justify-content: center;
    margin: 0 auto 20px; position: relative; z-index: 1;
    box-shadow: 0 6px 24px rgba(231,74,26,0.35);
  }
  .step-period {
    font-size: 11px; font-weight: 700; letter-spacing: 0.08em;
    text-transform: uppercase; color: var(--orange); margin-bottom: 8px;
  }
  .step-title {
    font-size: 18px; font-weight: 800; color: var(--text);
    letter-spacing: -0.01em; margin-bottom: 10px;
  }
  .step-body { font-size: 14px; color: var(--muted); line-height: 1.72; }

  /* ─── VS TABLE ─── */
  .vs-section {
    background: var(--cream-dark); border-top: var(--border); border-bottom: var(--border);
    padding: 88px 48px;
  }
  .vs-inner { max-width: 960px; margin: 0 auto; }
  .vs-header { text-align: center; margin-bottom: 48px; }
  .vs-title {
    font-size: clamp(24px, 3vw, 38px); font-weight: 900;
    letter-spacing: -0.025em; color: var(--text); margin-bottom: 10px;
  }
  .vs-sub { font-size: 15px; color: var(--muted); }
  .vs-table {
    width: 100%; border-collapse: separate; border-spacing: 0;
    border: var(--border); border-radius: 14px; overflow: hidden;
  }
  .vs-table th {
    padding: 14px 22px; font-size: 11px; font-weight: 700;
    letter-spacing: 0.08em; text-transform: uppercase; text-align: left;
  }
  .vs-table th:first-child { background: rgba(0,0,0,0.04); color: var(--muted); border-right: var(--border); width: 26%; }
  .vs-table th.col-bad { background: rgba(0,0,0,0.04); color: var(--muted); border-right: var(--border); }
  .vs-table th.col-good { background: rgba(231,74,26,0.06); color: var(--orange); }
  .vs-table td {
    padding: 15px 22px; font-size: 13.5px;
    border-top: var(--border); line-height: 1.5; vertical-align: middle;
  }
  .vs-table td:first-child {
    font-weight: 600; background: rgba(0,0,0,0.02);
    border-right: var(--border); color: var(--text);
  }
  .vs-table td.bad { color: var(--muted); border-right: var(--border); }
  .vs-table td.good { color: var(--text); font-weight: 500; }
  .chk { color: var(--orange); font-weight: 700; margin-right: 5px; }
  .crs { color: #ccc; margin-right: 5px; }

  /* ─── TESTIMONIALS ─── */
  .testi-section {
    padding: 88px 48px; background: var(--cream); border-top: var(--border);
  }
  .testi-inner { max-width: 1160px; margin: 0 auto; }
  .testi-header { text-align: center; margin-bottom: 48px; }
  .testi-title {
    font-size: clamp(24px, 3vw, 36px); font-weight: 900;
    letter-spacing: -0.025em; color: var(--text); margin-bottom: 8px;
  }
  .testi-sub { font-size: 14px; color: var(--muted); }

  /* ─── CTA SECTION ─── */
  .cta-section {
    background:
      radial-gradient(ellipse 55% 65% at 95% 100%, rgba(231,74,26,0.18), transparent 60%),
      radial-gradient(ellipse 45% 45% at 5% 10%, rgba(23,72,139,0.12), transparent 55%),
      #101010;
    color: #fff; padding: 100px 48px;
    text-align: center;
  }
  .cta-inner { max-width: 720px; margin: 0 auto; }
  .cta-title {
    font-size: clamp(34px, 5vw, 62px); font-weight: 900;
    letter-spacing: -0.035em; line-height: 1.06; color: #fff; margin-bottom: 20px;
  }
  .cta-sub {
    font-size: 17px; color: rgba(255,255,255,0.52);
    max-width: 520px; margin: 0 auto 40px; line-height: 1.72;
  }
  .cta-btn {
    background: var(--orange); color: #fff; font-weight: 700; font-size: 16px;
    padding: 16px 36px; border-radius: 1000px;
    display: inline-flex; align-items: center; gap: 10px;
    transition: opacity 0.2s, transform 0.2s;
    position: relative; overflow: hidden;
  }
  .cta-btn:hover { opacity: 0.88; transform: translateY(-2px); }
  .cta-btn::after {
    content: ''; position: absolute; top: 0; left: -90%; width: 55%; height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.28), transparent);
    transform: skewX(-20deg); pointer-events: none;
  }
  .cta-btn:hover::after { animation: shimmer 0.6s ease forwards; }
  .cta-note { margin-top: 16px; font-size: 13px; color: rgba(255,255,255,0.3); }
  .cal-embed { margin-top: 52px; }
  #dtc-cal { min-height: 500px; }

  /* ─── FOOTER ─── */
  .footer {
    background: #0a0a0a; border-top: 1px solid rgba(255,255,255,0.07);
    padding: 28px 48px;
    display: flex; align-items: center; justify-content: space-between;
    gap: 16px; flex-wrap: wrap;
  }
  .footer-logo { font-size: 14px; font-weight: 700; color: rgba(255,255,255,0.5); }
  .footer-links { display: flex; gap: 24px; flex-wrap: wrap; }
  .footer-link { font-size: 13px; color: rgba(255,255,255,0.3); transition: color 0.2s; }
  .footer-link:hover { color: var(--orange); }

  /* ─── REVEAL ANIMATIONS ─── */
  .reveal {
    opacity: 0; transform: translateY(52px); will-change: opacity, transform;
    transition: opacity 0.95s cubic-bezier(0.16, 1, 0.3, 1), transform 0.95s cubic-bezier(0.16, 1, 0.3, 1);
    transition-delay: var(--delay, 0ms);
  }
  .reveal.in-view { opacity: 1; transform: translateY(0); }
  .reveal-scale {
    opacity: 0; transform: scale(0.8) translateY(36px); will-change: opacity, transform;
    transition: opacity 0.72s cubic-bezier(0.34, 1.56, 0.64, 1), transform 0.72s cubic-bezier(0.34, 1.56, 0.64, 1);
    transition-delay: var(--delay, 0ms);
  }
  .reveal-scale.in-view { opacity: 1; transform: scale(1) translateY(0); }
  .reveal-blur {
    opacity: 0; transform: translateY(36px); filter: blur(16px); will-change: opacity, transform, filter;
    transition: opacity 1.15s cubic-bezier(0.16, 1, 0.3, 1), transform 1.15s cubic-bezier(0.16, 1, 0.3, 1), filter 0.95s cubic-bezier(0.16, 1, 0.3, 1);
    transition-delay: var(--delay, 0ms);
  }
  .reveal-blur.in-view { opacity: 1; transform: translateY(0); filter: blur(0); }

  /* ─── RESPONSIVE ─── */
  @media (max-width: 1024px) {
    .pain-grid { grid-template-columns: 1fr 1fr; }
    .solution-grid { grid-template-columns: 1fr; }
    .cost-grid { grid-template-columns: 1fr; gap: 16px; }
    .process-steps { grid-template-columns: 1fr; }
    .process-connector { display: none; }
    .pain-item:nth-child(n) { border-radius: 0; }
    .pain-item:first-child { border-radius: 12px 12px 0 0; }
    .pain-item:last-child { border-radius: 0 0 12px 12px; }
  }
  @media (max-width: 768px) {
    .nav { padding: 0 20px; }
    .nav-links { display: none; }
    .hero { padding: 56px 24px 48px; }
    .pain-section, .solution-section, .cta-section { padding: 72px 24px; }
    .cost-section, .process-section, .vs-section, .testi-section { padding: 64px 24px; }
    .pain-grid { grid-template-columns: 1fr; }
    .footer { padding: 24px; flex-direction: column; align-items: flex-start; }
    .hero-title { font-size: clamp(34px, 9vw, 52px); }
  }
</style>

<!-- Scroll Progress -->
<div class="scroll-progress" id="scroll-progress"></div>

<!-- NAV -->
<nav class="nav">
  <a href="/" class="nav-logo">Hichem Bennaceur</a>
  <ul class="nav-links">
    <li><a href="#what-i-fix">What I fix</a></li>
    <li><a href="#how-it-works">Process</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
  <a href="#contact" class="nav-cta">Book a free call →</a>
</nav>

<!-- ═══════════════════════════════════════ HERO ═══════════════════════════════════════ -->
<section class="hero">
  <span class="hero-tag">
    <span class="hero-tag-dot"></span>
    CRO Retainer for DTC Brands
  </span>
  <h1 class="hero-title reveal-blur" style="--delay:60ms">
    Your traffic is fine.<br>
    Your <span class="orange">conversion rate</span> <span class="underline-accent">isn't</span>.
  </h1>
  <p class="hero-sub reveal" style="--delay:180ms">
    I run structured CRO experiments for DTC brands — building a hypothesis-driven test backlog,
    executing statistically valid A/B tests, and iterating on every result.
    More revenue from the traffic you're already paying for.
  </p>
  <div class="hero-actions reveal" style="--delay:280ms">
    <a href="#contact" class="btn-primary">Book a free CRO audit →</a>
    <a href="#what-i-fix" class="btn-ghost">See how it works ↓</a>
  </div>
  <div class="trust-bar reveal" style="--delay:380ms">
    <span class="trust-stars">★★★★★</span>
    <span>Rated 4.8/5 · Trusted by DTC brands across USA, UK, Malaysia & Europe</span>
  </div>
</section>

<!-- ═══════════════════════════════════════ PAIN ═══════════════════════════════════════ -->
<section class="pain-section" id="what-i-fix">
  <div class="pain-inner">
    <p class="eyebrow reveal" style="--delay:0ms">Sound familiar?</p>
    <h2 class="pain-title reveal-blur" style="--delay:80ms">
      More ad spend. Same conversion rate.<br>You're funding a leaky bucket.
    </h2>

    <div class="pain-grid">
      <div class="pain-item reveal-scale" style="--delay:0ms">
        <div class="pain-num">01</div>
        <h3 class="pain-item-title">You're spending more on ads. Revenue isn't scaling with it.</h3>
        <p class="pain-item-body">Your checkout converts at 1.8%. Top-quartile DTC brands convert at 4.5%. That gap isn't a creative problem — it's a conversion problem. And every pound you pour into ads widens it.</p>
      </div>
      <div class="pain-item reveal-scale" style="--delay:80ms">
        <div class="pain-num">02</div>
        <h3 class="pain-item-title">You've tried "CRO". It didn't move the needle.</h3>
        <p class="pain-item-body">You changed a button color. Rewrote a headline. Maybe ran one A/B test that was never statistically significant. That's not CRO — that's guessing with extra steps. Real experimentation is a system, not a one-off.</p>
      </div>
      <div class="pain-item reveal-scale" style="--delay:160ms">
        <div class="pain-num">03</div>
        <h3 class="pain-item-title">Your product page gets traffic. It doesn't convert visitors into buyers.</h3>
        <p class="pain-item-body">Visitors scroll. They hesitate. They leave. You can see the drop-off — you can't see why. Without a 'why', every change you ship is a coin flip that wastes your developer's time and proves nothing.</p>
      </div>
      <div class="pain-item reveal-scale" style="--delay:240ms">
        <div class="pain-num">04</div>
        <h3 class="pain-item-title">Your agency A/B tests ad creatives. Nobody tests your website.</h3>
        <p class="pain-item-body">Media buyers optimize for click-through rate. Nobody owns what happens after the click — which is where 65–75% of your potential customers vanish. That's the gap nobody's plugging.</p>
      </div>
      <div class="pain-item reveal-scale" style="--delay:320ms">
        <div class="pain-num">05</div>
        <h3 class="pain-item-title">You don't have a test backlog. You have a list of opinions.</h3>
        <p class="pain-item-body">"Let's try a shorter checkout." "Maybe the photos need updating." "Add a countdown timer." Untested opinions ≠ CRO. Every change needs a hypothesis, a measurement plan, and a result that feeds the next test.</p>
      </div>
      <div class="pain-item reveal-scale" style="--delay:400ms">
        <div class="pain-num">06</div>
        <h3 class="pain-item-title">You can't trust your test results anyway.</h3>
        <p class="pain-item-body">Underpowered tests. No pre-defined sample size. Peeking at results before significance. Most DTC brands run invalid A/B tests and make permanent site changes based on noise — then wonder why nothing's improving.</p>
      </div>
    </div>

    <div class="pain-callout reveal" style="--delay:0ms">
      <div class="pain-callout-icon"><i data-lucide="zap"></i></div>
      <p class="pain-callout-text">
        <strong>This isn't a traffic problem. You're getting the clicks.</strong>
        The problem is you're converting a fraction of the people you could —
        and without a structured experimentation system, that gap never closes.
        Every month of inaction is revenue you already paid to acquire, quietly walking out the door.
      </p>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════ COST STATS ═══════════════════════════════════════ -->
<section class="cost-section">
  <div class="cost-inner">
    <div class="cost-header reveal-blur">
      <p class="eyebrow">The conversion gap</p>
      <h2 class="cost-title">The upside most DTC brands are leaving on the table</h2>
      <p class="cost-sub">The difference between average and top-quartile isn't ad spend. It's conversion rate.</p>
    </div>
    <div class="cost-grid">
      <div class="cost-card reveal-scale" style="--delay:0ms">
        <div class="cost-card-num">1.8% → 4.5%</div>
        <div class="cost-card-title">average vs. top-quartile DTC conversion rate</div>
        <p class="cost-card-body">Doubling your conversion rate doubles revenue from the same traffic. The brands at 4.5% aren't running better ads — they're running a structured CRO program that compounds every sprint.</p>
      </div>
      <div class="cost-card reveal-scale" style="--delay:100ms">
        <div class="cost-card-num">3–5×</div>
        <div class="cost-card-title">ROI from CRO vs. equivalent ad spend increase</div>
        <p class="cost-card-body">Spending more on ads to grow revenue is expensive. Improving conversion extracts more from traffic you already paid for — at a fraction of the cost per additional purchase.</p>
      </div>
      <div class="cost-card reveal-scale" style="--delay:200ms">
        <div class="cost-card-num">72%</div>
        <div class="cost-card-title">of A/B tests fail due to invalid methodology</div>
        <p class="cost-card-body">No pre-defined hypothesis. No minimum detectable effect. Peeking at results. Most DTC "CRO" produces noise, not signal. That's the first thing I fix before touching a single test.</p>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════ SOLUTION ═══════════════════════════════════════ -->
<section class="solution-section">
  <div class="solution-inner">
    <div class="solution-header reveal-blur">
      <p class="eyebrow">What I deliver</p>
      <h2 class="solution-title">A full experimentation program. Not just "some A/B tests".</h2>
      <p class="solution-sub">From behavioral research to running experiments to learning from every result — a structured CRO engine that compounds month over month.</p>
    </div>
    <div class="solution-grid">

      <div class="solution-card reveal-scale" style="--delay:0ms">
        <div class="solution-icon"><i data-lucide="flask-conical"></i></div>
        <h3 class="solution-card-title">The Experimentation Engine</h3>
        <p class="solution-card-body">Research → Hypothesize → Prioritize → Test → Learn → Repeat. This is the loop that actually moves conversion rates. Not random tests — a structured backlog of high-impact hypotheses, executed with statistical rigor, and fed back into the next sprint.</p>
        <ul class="deliverables">
          <li>Behavioral research: heatmaps, session recordings, funnel analysis</li>
          <li>ICE-scored hypothesis backlog with documented rationale per test</li>
          <li>A/B test design: variant brief, sample size, MDE, success metrics</li>
          <li>Statistical significance monitoring — no peeking, no false positives</li>
          <li>Win/loss analysis: every result dissected and fed into the next round</li>
          <li>Monthly experimentation report: velocity, learnings, and revenue impact</li>
        </ul>
      </div>

      <div class="solution-card reveal-scale" style="--delay:100ms">
        <div class="solution-icon"><i data-lucide="search"></i></div>
        <h3 class="solution-card-title">Conversion Deep Dive</h3>
        <p class="solution-card-body">Before testing anything, I map exactly where and why shoppers drop. Every friction point, every hesitation moment, every exit — quantified and ranked. This is what separates evidence-based CRO from opinion-driven redesigns.</p>
        <ul class="deliverables">
          <li>Full-funnel heatmap and scroll depth analysis per key page</li>
          <li>Session recording review: rage clicks, exit intent, confusion patterns</li>
          <li>Exit survey implementation — the "why" missing from your analytics</li>
          <li>Micro-funnel analysis: ad click → PDP → cart → checkout, step by step</li>
          <li>Prioritized friction map with revenue estimates per fix</li>
          <li>Customer survey: what almost stopped them from completing the purchase</li>
        </ul>
      </div>

      <div class="solution-card reveal-scale" style="--delay:200ms">
        <div class="solution-icon"><i data-lucide="radio"></i></div>
        <h3 class="solution-card-title">Page & Checkout Optimization</h3>
        <p class="solution-card-body">Your product pages and checkout are where you win or lose. I run structured A/B tests across copy, layout, trust signals, and UX — backed by behavioral data from the deep dive, not design opinions.</p>
        <ul class="deliverables">
          <li>PDP test roadmap: headline, imagery, social proof, price anchoring, urgency</li>
          <li>Checkout flow optimization — the single highest-ROI page on your site</li>
          <li>Cart abandonment: root cause identified, first test designed and queued</li>
          <li>Post-purchase flow: upsell and cross-sell conversion tests</li>
          <li>Mobile-specific variants — where most DTC traffic converts worst</li>
          <li>Trust signal testing: reviews placement, badges, guarantees, shipping copy</li>
        </ul>
      </div>

      <div class="solution-card reveal-scale" style="--delay:300ms">
        <div class="solution-icon"><i data-lucide="bar-chart-2"></i></div>
        <h3 class="solution-card-title">The Data Foundation That Makes Tests Valid</h3>
        <p class="solution-card-body">You can't run trustworthy CRO experiments on broken tracking. Before we test anything, I audit your analytics setup to make sure every event fires accurately — so every result you see is signal, not noise.</p>
        <ul class="deliverables">
          <li>GA4 + server-side event audit and rebuild where needed</li>
          <li>Shopify + GTM: add-to-cart, checkout steps, purchase — all firing clean</li>
          <li>A/B test tool data layer check — so variants read accurate user data</li>
          <li>Attribution audit: which channels are actually sending convertible traffic</li>
          <li>Dashboard: true conversion rate, AOV, repeat purchase, and net revenue</li>
          <li>Weekly insight digest — what changed, what it means, what to test next</li>
        </ul>
      </div>

    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════ PROCESS ═══════════════════════════════════════ -->
<section class="process-section" id="how-it-works">
  <div class="process-inner">
    <div class="process-header reveal-blur">
      <p class="eyebrow">How it works</p>
      <h2 class="process-title">From first look to compounding results — in 90 days.</h2>
      <p class="process-sub">Sprint-based. You have a prioritized test backlog before the end of week two.</p>
    </div>
    <div class="process-steps">
      <div class="process-connector"></div>

      <div class="process-step reveal-scale" style="--delay:0ms">
        <div class="step-num">1</div>
        <div class="step-period">Week 1–2</div>
        <h3 class="step-title">Research & Diagnose</h3>
        <p class="step-body">I audit your conversion funnel end-to-end — behavioral data, session recordings, funnel metrics, and customer feedback. You get a prioritized map of where you're losing revenue and exactly why. Not vague recommendations. Specific leaks with estimated revenue impact.</p>
      </div>

      <div class="process-step reveal-scale" style="--delay:100ms">
        <div class="step-num">2</div>
        <div class="step-period">Weeks 3–5</div>
        <h3 class="step-title">Build the Test Backlog</h3>
        <p class="step-body">I translate research into a scored experimentation backlog — every hypothesis documented, every test designed with a clear success metric, minimum detectable effect, and required sample size. No more guessing what to test. No more random button-color changes.</p>
      </div>

      <div class="process-step reveal-scale" style="--delay:200ms">
        <div class="step-num">3</div>
        <div class="step-period">Month 2–3+</div>
        <h3 class="step-title">Test, Learn, Compound</h3>
        <p class="step-body">Structured A/B tests run continuously — each result feeding the next sprint. Every win gets rolled out. Every loss gets dissected for learnings. Conversion rates compound. You extract more revenue from the same traffic, every single month.</p>
      </div>

    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════ VS TABLE ═══════════════════════════════════════ -->
<section class="vs-section">
  <div class="vs-inner">
    <div class="vs-header reveal-blur">
      <p class="eyebrow">Why me, not an agency</p>
      <h2 class="vs-title">What a real CRO program looks like</h2>
      <p class="vs-sub">Not creative opinions. Not vanity metrics. A structured experimentation system that lifts revenue.</p>
    </div>
    <table class="vs-table reveal-scale" style="--delay:0ms">
      <thead>
        <tr>
          <th></th>
          <th class="col-bad">Typical Agency</th>
          <th class="col-good">Working with Hichem</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>CRO methodology</td>
          <td class="bad"><span class="crs">✗</span>Opinion-based redesigns, no hypothesis</td>
          <td class="good"><span class="chk">✓</span>Hypothesis-driven, ICE-scored, statistically valid</td>
        </tr>
        <tr>
          <td>Test backlog</td>
          <td class="bad"><span class="crs">✗</span>Random ideas, no priority framework</td>
          <td class="good"><span class="chk">✓</span>Scored by revenue impact × confidence × effort</td>
        </tr>
        <tr>
          <td>Who runs tests</td>
          <td class="bad"><span class="crs">✗</span>Junior team member, if anyone</td>
          <td class="good"><span class="chk">✓</span>Me — with pattern library from 50+ DTC clients</td>
        </tr>
        <tr>
          <td>Test validity</td>
          <td class="bad"><span class="crs">✗</span>No sample size plan, peeking at results</td>
          <td class="good"><span class="chk">✓</span>Pre-defined MDE, significance threshold, holdouts</td>
        </tr>
        <tr>
          <td>Analytics foundation</td>
          <td class="bad"><span class="crs">✗</span>Whatever's already there — gaps included</td>
          <td class="good"><span class="chk">✓</span>Audited, server-side, clean event tracking</td>
        </tr>
        <tr>
          <td>Reporting</td>
          <td class="bad"><span class="crs">✗</span>Platform ROAS, CTR, impressions</td>
          <td class="good"><span class="chk">✓</span>True conversion lift, AOV, net revenue per test</td>
        </tr>
        <tr>
          <td>Access</td>
          <td class="bad"><span class="crs">✗</span>Account manager + 48–72hr response</td>
          <td class="good"><span class="chk">✓</span>Direct, async-first, transparent on every decision</td>
        </tr>
      </tbody>
    </table>
  </div>
</section>

<!-- ═══════════════════════════════════════ TESTIMONIALS ═══════════════════════════════════════ -->
<section class="testi-section">
  <div class="testi-inner">
    <div class="testi-header reveal-blur">
      <h2 class="testi-title">Don't take my word for it</h2>
      <p class="testi-sub">★★★★★ &nbsp; Rated 4.8/5 from 26+ clients</p>
    </div>
    <script src="https://widget.senja.io/widget/fccad927-8689-4142-afd3-6549b0aa06fa/platform.js" type="text/javascript" async></script>
    <div class="senja-embed" data-id="fccad927-8689-4142-afd3-6549b0aa06fa" data-mode="shadow" data-lazyload="false" style="display:block;width:100%;"></div>
  </div>
</section>

<!-- ═══════════════════════════════════════ CTA ═══════════════════════════════════════ -->
<section class="cta-section" id="contact">
  <div class="cta-inner">
    <p class="eyebrow reveal" style="--delay:0ms; color:rgba(231,74,26,0.8)">Let's talk</p>
    <h2 class="cta-title reveal-blur" style="--delay:80ms">
      Let's find out what your conversion rate should be.
    </h2>
    <p class="cta-sub reveal" style="--delay:180ms">
      Book a free 30-minute CRO audit. I'll review your funnel, identify your biggest
      conversion leaks, and give you a prioritized list of what to test first —
      no pitch, no commitment.
    </p>
    <div class="reveal" style="--delay:260ms">
      <a href="https://cal.com/hichem/30min" class="cta-btn">
        Book my free CRO audit →
      </a>
      <p class="cta-note">30 minutes. I'll tell you exactly where you're losing revenue — and what test to run first.</p>
    </div>
    <div class="cal-embed reveal" style="--delay:320ms">
      <div id="dtc-cal"></div>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer class="footer">
  <span class="footer-logo">Hichem Bennaceur</span>
  <div class="footer-links">
    <a href="/" class="footer-link">← Back to home</a>
    <a href="/#services" class="footer-link">Services</a>
    <a href="/#why" class="footer-link">About</a>
    <a href="/#audit" class="footer-link">Book a call</a>
  </div>
</footer>

<!-- Lucide Icons -->
<script src="https://unpkg.com/lucide@latest/dist/umd/lucide.min.js"></script>

<!-- Cal.com embed -->
<script type="text/javascript">
  (function (C, A, L) {
    let p = function (a, ar) { a.q.push(ar); };
    let d = C.document;
    C.Cal = C.Cal || function () {
      let cal = C.Cal; let ar = arguments;
      if (!cal.loaded) { cal.ns = {}; cal.q = cal.q || []; d.head.appendChild(d.createElement("script")).src = A; cal.loaded = true; }
      if (ar[0] === L) { const api = function () { p(api, arguments); }; const namespace = ar[1]; api.q = api.q || []; if(typeof namespace === "string"){ cal.ns[namespace] = cal.ns[namespace] || api; p(cal.ns[namespace], ar); p(cal, ["initNamespace", namespace]); } else p(cal, ar); return; }
      p(cal, ar);
    };
  })(window, "https://app.cal.com/embed/embed.js", "init");

  Cal("init", "dtc30min", {origin:"https://app.cal.com"});
  Cal.ns["dtc30min"]("inline", {
    elementOrSelector: "#dtc-cal",
    config: {"layout":"week_view","useSlotsViewOnSmallScreen":"true"},
    calLink: "hichem/30min",
  });
  Cal.ns["dtc30min"]("ui", {
    "cssVarsPerTheme": {"light":{"cal-brand":"#E74A1A"},"dark":{"cal-brand":"#E74A1A"}},
    "hideEventTypeDetails": false,
    "layout": "column_view",
    "theme": "light"
  });
</script>

<!-- Page scripts -->
<script>
  // Lucide icons
  if (typeof lucide !== 'undefined') lucide.createIcons();

  // Scroll progress bar
  const bar = document.getElementById('scroll-progress');
  window.addEventListener('scroll', () => {
    const pct = (window.scrollY / (document.documentElement.scrollHeight - window.innerHeight)) * 100;
    if (bar) bar.style.width = pct + '%';
  }, { passive: true });

  // Intersection Observer — scroll reveals
  const obs = new IntersectionObserver(
    (entries) => entries.forEach(e => {
      if (e.isIntersecting) { e.target.classList.add('in-view'); obs.unobserve(e.target); }
    }),
    { threshold: 0.07, rootMargin: '0px 0px -28px 0px' }
  );
  document.querySelectorAll('.reveal, .reveal-scale, .reveal-blur').forEach(el => obs.observe(el));

  // Fire hero animations immediately
  document.querySelectorAll('.hero .reveal, .hero .reveal-blur').forEach(el => {
    const delay = parseInt(el.style.getPropertyValue('--delay') || '0') || 0;
    setTimeout(() => el.classList.add('in-view'), delay);
  });
  // Fire hero tag too
  document.querySelector('.hero-tag')?.classList.add('in-view');

  // 3D tilt on cards
  document.querySelectorAll('.solution-card, .cost-card, .pain-item').forEach(card => {
    card.addEventListener('mousemove', e => {
      const r = card.getBoundingClientRect();
      const dx = (e.clientX - r.left - r.width / 2) / (r.width / 2);
      const dy = (e.clientY - r.top - r.height / 2) / (r.height / 2);
      card.style.transition = 'transform 0.05s ease';
      card.style.transform = `perspective(900px) rotateY(${dx * 5}deg) rotateX(${-dy * 5}deg) translateZ(5px)`;
    });
    card.addEventListener('mouseleave', () => {
      card.style.transition = 'transform 0.45s cubic-bezier(0.16, 1, 0.3, 1)';
      card.style.transform = '';
    });
  });
</script>

</Layout>
